# tests

This folder contains tests for case explainer

## run tests

```
python -m pytest tests/ -v
```

## run tests with coverage

```
python -m pytest tests/ --cov=case_explainer --cov-report=term-missing
```

